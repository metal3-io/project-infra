apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: prow
  name: oci-jenkins-operator
spec:
  template:
    spec:
      containers:
      - name: oci-jenkins-operator
        resources:
          requests:
            cpu: 50m
            memory: 50Mi
