apiVersion: v1
kind: Service
metadata:
  name: oci-jenkins-operator
  namespace: prow
spec:
  selector:
    jenkins_operator: oci
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: prow
  name: oci-jenkins-operator
  labels:
    jenkins_operator: oci
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      jenkins_operator: oci
  template:
    metadata:
      labels:
        jenkins_operator: oci
    spec:
      serviceAccountName: oci-jenkins-operator
      containers:
      - name: oci-jenkins-operator
        image: us-docker.pkg.dev/k8s-infra-prow/images/jenkins-operator:v20260729-7f8580d8d
        args:
        - --config-path=/etc/config/config.yaml
        - --job-config-path=/etc/job-config
        - --jenkins-url=https://jenkins.apps.test.metal3.io
        - --jenkins-user=prow
        - --jenkins-token-file=/etc/jenkins/token
        - --github-token-path=/etc/github/token
        - --github-endpoint=http://ghproxy
        - --github-endpoint=https://api.github.com
        - --label-selector=jenkins_operator=oci
        - --skip-report=false
        - --dry-run=false
        ports:
        # Used for serving logs so that they can be displayed by deck
        - name: http
          containerPort: 8080
        volumeMounts:
        - name: github-token
          mountPath: /etc/github
          readOnly: true
        - name: oci-jenkins-token
          mountPath: /etc/jenkins
          readOnly: true
        - name: config
          mountPath: /etc/config
          readOnly: true
        - name: job-config
          mountPath: /etc/job-config
          readOnly: true
      volumes:
      - name: github-token
        secret:
          secretName: github-token
      - name: oci-jenkins-token
        secret:
          secretName: oci-jenkins-token
      - name: config
        configMap:
          name: config
      - name: job-config
        configMap:
          name: job-config
---
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: prow
  name: oci-jenkins-operator
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: prow
  name: oci-jenkins-operator
rules:
- apiGroups:
  - "prow.k8s.io"
  resources:
  - prowjobs
  verbs:
  - get
  - list
  - watch
  - update
  - patch
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: prow
  name: oci-jenkins-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: oci-jenkins-operator
subjects:
- kind: ServiceAccount
  name: oci-jenkins-operator
